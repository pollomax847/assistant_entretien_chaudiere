{
  "name": "chauffage-expert",
  "version": "1.0.0",
  "description": "Application d'assistance pour l'entretien des chaudières",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -r css public/ && cp -r js public/ && cp -r assets public/ && cp index.html public/ && cp .htaccess public/ && mkdir -p public/assets/icons && cp assets/icons/* public/assets/icons/",
    "start": "serve public"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
